/* 用户信息 */
export default {
	// 
	set_userWone(state, user) {
		state.userWone = user;
	},

	set_parentUrl(state, url) {
		state.parentUrl = url;
	},

	set_teacherUrl(state, url) {
		state.teacherUrl = url;
	},

	set_hasHistory(state, val) {
		state.hasHistory = val;
	},
}
